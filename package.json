{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:panel": "yarn workspace react-fiber-visualizer-panel start",
    "build:graph": "yarn workspace fiber-tree-graph build"
  }
}